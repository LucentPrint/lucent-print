"use client";import{useState}from"react";export default function Page(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/affiliate/apply",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({website:f.get("website"),audience:f.get("audience")})});const d=await r.json();setMsg(d.ok?"Application received.":d.error)}return <section className="section"><div className="shell grid gap-10 lg:grid-cols-2"><div><p className="eyebrow">Scale</p><h1 className="title my-6">Affiliate & Referral Program</h1><p className="muted text-lg">Apply to share Lucent Print, track qualified referrals, and earn commission on eligible sales.</p></div><form onSubmit={submit} className="glass grid gap-4 rounded-3xl p-7"><input name="website" className="input" placeholder="Website or social profile"/><textarea name="audience" className="input min-h-32" placeholder="Tell us about your audience" required/><button className="btn btn-primary">Apply</button><p className="muted">{msg}</p></form></div></section>}
