"use client";import{useState}from"react";export default function Page(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/wholesale/quote",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({businessName:f.get("businessName"),items:[{description:f.get("items"),quantity:Number(f.get("quantity"))}],estimatedTotal:0})});const d=await r.json();setMsg(d.ok?"Quote request received.":d.error)}return <section className="section"><div className="shell grid gap-10 lg:grid-cols-2"><div><p className="eyebrow">Wholesale</p><h1 className="title my-6">Bulk orders built for your business.</h1><p className="muted text-lg">Request business pricing, custom colors, event quantities, and reseller packages.</p></div><form onSubmit={submit} className="glass grid gap-4 rounded-3xl p-7"><input name="businessName" className="input" placeholder="Business name" required/><textarea name="items" className="input min-h-32" placeholder="Products or custom items" required/><input name="quantity" type="number" min="10" className="input" placeholder="Estimated quantity" required/><button className="btn btn-primary">Request quote</button><p className="muted">{msg}</p></form></div></section>}
