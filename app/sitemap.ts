import type{MetadataRoute}from"next";import{getProducts}from"@/lib/data";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";const routes=['','/shop','/design-vault','/print-lab','/custom-studio','/loyalty','/affiliate','/wholesale','/subscriptions'];const ps=await getProducts();return[...routes.map(url=>({url:base+url,lastModified:new Date()})),...ps.map(p=>({url:`${base}/products/${p.slug}`,lastModified:new Date()}))]}
