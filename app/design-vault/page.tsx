import{getDesignVaultItems}from"@/lib/data";import{VaultClient}from"./vault-client";export const metadata={title:"The Design Vault"};export default async function Page(){const items=await getDesignVaultItems();return <section className="section"><div className="shell"><p className="eyebrow">Community-powered releases</p><h1 className="title my-6">The Design Vault</h1><p className="muted mb-10 max-w-3xl text-lg">Vote on concepts, follow prototypes, and help decide what Lucent Print makes next.</p><VaultClient items={items}/></div></section>}
