"use client";import{useState}from"react";import type{Product}from"@/lib/types";import{useCart}from"@/components/cart-provider";export function AddButton({product}:{product:Product}){const{add}=useCart();const[quantity,setQuantity]=useState(1);return <div className="mt-8 grid grid-cols-[110px_1fr] gap-3"><label className="grid gap-1 text-sm font-bold">Quantity<input aria-label="Quantity" className="input text-center" type="number" min="1" max="99" value={quantity} onChange={event=>setQuantity(Math.max(1,Math.min(99,Number(event.target.value)||1)))}/></label><button onClick={()=>add(product,undefined,quantity)} className="btn btn-primary self-end">Add {quantity} to cart</button></div>}
