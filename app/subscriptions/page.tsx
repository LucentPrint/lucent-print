"use client";import{useState}from"react";export default function Page(){const[msg,setMsg]=useState("");async function join(plan:string){const r=await fetch("/api/subscriptions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({plan})});const d=await r.json();setMsg(d.ok?"You are on the interest list.":d.error)}return <section className="section"><div className="shell"><p className="eyebrow">Lucent Drop Box</p><h1 className="title my-6">A new collectible drop, delivered.</h1><div className="grid-auto">{[["seasonal","Seasonal Box","Holiday and limited-drop designs"],["monthly","Monthly Creator Box","A surprise clicker, sensory item, and exclusive color"],["vip","VIP Collector Box","Premium drops and early access"]].map(([id,name,copy])=><div key={id} className="glass rounded-2xl p-6"><h2 className="text-2xl font-black">{name}</h2><p className="muted my-4">{copy}</p><button onClick={()=>join(id)} className="btn btn-primary">Join interest list</button></div>)}</div><p className="mt-6 text-pink-300">{msg}</p></div></section>}
