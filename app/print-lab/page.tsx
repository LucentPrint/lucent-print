import{getPrinters}from"@/lib/data";export const metadata={title:"Print Lab 2.0"};export default async function Page(){const printers=await getPrinters();return <section className="section"><div className="shell"><p className="eyebrow">Live production</p><h1 className="title my-6">Print Lab 2.0</h1><p className="muted mb-10 max-w-3xl text-lg">Production status from the Lucent Print studio.</p><div className="grid-auto">{printers.map(p=><article key={p.id} className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><h2 className="text-2xl font-black">{p.name}</h2><span className="badge">{p.status}</span></div><p className="muted my-4">{p.currentJob}</p><div className="h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-gradient-to-r from-blue-500 to-pink-500" style={{width:`${p.progress}%`}}/></div><p className="mt-2 text-right font-black">{p.progress}%</p></article>)}</div></div></section>}
