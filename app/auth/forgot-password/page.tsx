"use client";import{useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[msg,setMsg]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get("email"));const s=createClient();if(!s)return setMsg("Supabase is not configured.");const{error}=await s.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/account`});setMsg(error?.message??"Password reset email sent.")}return <section className="section"><form onSubmit={go} className="glass mx-auto grid max-w-md gap-4 rounded-3xl p-8"><p className="eyebrow">Account recovery</p><h1 className="text-4xl font-black">Reset password</h1><input className="input" name="email" type="email" required placeholder="Email"/><button className="btn btn-primary">Send reset link</button><p className="muted">{msg}</p></form></section>}
