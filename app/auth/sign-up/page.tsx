"use client";import Link from"next/link";import{useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[msg,setMsg]=useState("");async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),email=String(f.get("email")),password=String(f.get("password")),fullName=String(f.get("fullName"));const s=createClient();if(!s)return setMsg("Add Supabase credentials to enable authentication.");const r=await s.auth.signUp({email,password,options:{data:{full_name:fullName}}});setMsg(r.error?.message??"Account created. Check your email if confirmation is enabled.")}return <section className="section"><form onSubmit={go} className="glass mx-auto grid max-w-md gap-4 rounded-3xl p-8"><p className="eyebrow">Lucent account</p><h1 className="text-4xl font-black">Create account</h1><input name="fullName" className="input" placeholder="Full name" required/><input name="email" type="email" className="input" placeholder="Email" required/><input name="password" type="password" minLength={8} className="input" placeholder="Password (8+ characters)" required/><button className="btn btn-primary">Create account</button><p className="muted">{msg}</p><Link href="/auth/sign-in" className="text-blue-400">Already have an account?</Link></form></section>}
