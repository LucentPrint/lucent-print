"use client";import Link from"next/link";import{useRouter}from"next/navigation";import{useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[msg,setMsg]=useState("");const router=useRouter();async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),email=String(f.get("email")),password=String(f.get("password"));const s=createClient();if(!s)return setMsg("Add Supabase credentials to enable authentication.");const r=await s.auth.signInWithPassword({email,password});if(!r.error){router.push("/account");router.refresh()}else setMsg(r.error.message)}return <section className="section"><form onSubmit={go} className="glass mx-auto grid max-w-md gap-4 rounded-3xl p-8"><p className="eyebrow">Lucent account</p><h1 className="text-4xl font-black">Sign in</h1><input name="email" type="email" className="input" placeholder="Email" required/><input name="password" type="password" className="input" placeholder="Password" required/><button className="btn btn-primary">Sign in</button><p className="muted">{msg}</p><div className="flex justify-between text-sm"><Link href="/auth/sign-up" className="text-blue-400">Create account</Link><Link href="/auth/forgot-password" className="text-blue-400">Forgot password?</Link></div></form></section>}
