"use client";import{useState}from"react";export default function Page(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/custom-orders',{method:'POST',body:f});const d=await r.json();setMsg(d.message??d.error)}return <section className="section"><div className="shell grid gap-12 lg:grid-cols-2"><div><p className="eyebrow">Custom Order Studio</p><h1 className="title my-6">Describe it. Customize it. Print it.</h1><p className="muted text-lg">Upload a reference image or STL, choose material and colors, add dimensions, quantity, deadline, and budget.</p></div><form onSubmit={submit} className="glass grid gap-4 rounded-3xl p-7"><input name="name" className="input" placeholder="Name" required/><input name="email" type="email" className="input" placeholder="Email" required/><textarea name="description" className="input min-h-32" placeholder="Describe your idea" required/><div className="grid grid-cols-2 gap-3"><input name="dimensions" className="input" placeholder="Dimensions"/><input name="quantity" type="number" className="input" placeholder="Quantity"/></div><select name="material" className="input"><option>Premium PLA</option><option>Glow PLA</option><option>Silk PLA</option><option>Other / unsure</option></select><input name="colors" className="input" placeholder="Preferred colors"/><input name="file" type="file" className="input" accept=".stl,.3mf,.obj,image/*"/><button className="btn btn-primary">Submit project request</button><p className="muted">{msg}</p></form></div></section>}
