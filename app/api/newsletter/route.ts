import{NextResponse}from"next/server";import{z}from"zod";import{createClient}from"@/lib/supabase/server";const S=z.object({email:z.string().email()});export async function POST(req:Request){const parsed=S.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Valid email required"},{status:400});const s=await createClient();if(s)await s.from('newsletter_subscribers').upsert({email:parsed.data.email,status:'subscribed'});return NextResponse.json({ok:true})}
