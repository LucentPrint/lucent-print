"use client";
import Image from"next/image";import Link from"next/link";import{Heart,ShoppingBag,Bell}from"lucide-react";import type{Product}from"@/lib/types";import{money}from"@/lib/commerce";import{useCart}from"./cart-provider";import{useWishlist}from"./wishlist-provider";
export function ProductCard({p}:{p:Product}){const{add}=useCart();const wishlist=useWishlist();const coming=p.status==="coming_soon"||p.price<=0;return <article className="glass card group"><div className="relative h-64 overflow-hidden"><Image src={p.images[0]} alt={p.name} fill sizes="(max-width: 768px) 100vw, 25vw" className="object-cover transition duration-500 group-hover:scale-105"/><button aria-label="Toggle wishlist" onClick={()=>wishlist.toggle(p)} className="absolute right-3 top-3 rounded-full bg-black/70 p-2"><Heart className={wishlist.has(p.id)?"fill-pink-500 text-pink-500":""}/></button><span className="badge absolute left-3 top-3">{p.status.replaceAll("_"," ")}</span></div><div className="p-5"><p className="eyebrow">{p.collection}</p><h3 className="my-2 text-xl font-black">{p.name}</h3><p className="muted min-h-12">{p.description}</p><div className="mt-5 flex items-center gap-3"><b className="text-xl">{coming?"Coming soon":money(p.price)}</b><Link className="ml-auto text-sm text-blue-400" href={`/products/${p.slug}`}>Details</Link><button disabled={coming} title={coming?"Coming soon":"Add to cart"} onClick={()=>add(p)} className="rounded-lg bg-white p-2 text-black disabled:cursor-not-allowed disabled:opacity-40">{coming?<Bell size={18}/>:<ShoppingBag size={18}/>}</button></div></div></article>}
