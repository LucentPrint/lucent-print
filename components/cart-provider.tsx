"use client";
import {createContext,useContext,useEffect,useMemo,useState} from "react";
import type{CartItem,Product}from"@/lib/types";
type C={items:CartItem[];add:(p:Product,color?:string)=>void;remove:(id:string)=>void;setQty:(id:string,q:number)=>void;clear:()=>void;count:number;subtotal:number;shipping:number;total:number;open:boolean;setOpen:(v:boolean)=>void};
const Ctx=createContext<C|null>(null);
export function CartProvider({children}:{children:React.ReactNode}){const[items,setItems]=useState<CartItem[]>([]);const[open,setOpen]=useState(false);useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("lucent-cart")||"[]"))}catch{}},[]);useEffect(()=>{localStorage.setItem("lucent-cart",JSON.stringify(items))},[items]);const add=(p:Product,color?:string)=>{if(p.status==="coming_soon"||p.status==="draft"||p.price<=0)return;setItems(x=>{const f=x.find(i=>i.product.id===p.id&&i.selectedColor===color);return f?x.map(i=>i===f?{...i,quantity:i.quantity+1}:i):[...x,{product:p,quantity:1,selectedColor:color}]});setOpen(true)};const remove=(id:string)=>setItems(x=>x.filter(i=>i.product.id!==id));const setQty=(id:string,q:number)=>setItems(x=>q<=0?x.filter(i=>i.product.id!==id):x.map(i=>i.product.id===id?{...i,quantity:q}:i));const clear=()=>setItems([]);const subtotal=items.reduce((a,i)=>a+i.product.price*i.quantity,0);const shipping=subtotal===0||subtotal>=75?0:6.95;const value=useMemo(()=>({items,add,remove,setQty,clear,count:items.reduce((a,i)=>a+i.quantity,0),subtotal,shipping,total:subtotal+shipping,open,setOpen}),[items,open,subtotal,shipping]);return <Ctx.Provider value={value}>{children}</Ctx.Provider>};
export const useCart=()=>{const c=useContext(Ctx);if(!c)throw new Error("CartProvider missing");return c};
