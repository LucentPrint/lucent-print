"use client";import Image from"next/image";import Link from"next/link";import{Menu,ShoppingBag,User}from"lucide-react";import{useState}from"react";import{useCart}from"./cart-provider";
export function Header(){const[m,setM]=useState(false);const{count,setOpen}=useCart();const closeMenu=()=>setM(false);return <header className="sticky top-0 z-50 border-b border-white/10 bg-black/70 backdrop-blur-xl"><div className="shell flex h-20 items-center gap-6"><Link href="/" onClick={closeMenu} className="mr-auto flex items-center gap-3"><Image src="/images/lucent-logo.webp" width={52} height={52} alt="Lucent Print logo" className="rounded-xl"/><span className="font-black tracking-[.18em]">LUCENT <small className="block text-[10px] tracking-[.3em] text-zinc-400">PRINT</small></span></Link><nav className={`${m?"flex":"hidden"} absolute left-4 right-4 top-20 flex-col gap-4 rounded-xl border border-white/10 bg-zinc-950 p-5 lg:static lg:flex lg:flex-row lg:bg-transparent lg:p-0`}><Link href="/shop" onClick={closeMenu}>Shop</Link><Link href="/design-vault" onClick={closeMenu}>Design Vault</Link><Link href="/print-lab" onClick={closeMenu}>Print Lab</Link><Link href="/custom-studio" onClick={closeMenu}>Custom Studio</Link><Link href="/wholesale" onClick={closeMenu}>Wholesale</Link><Link href="/admin" onClick={closeMenu}>Admin</Link></nav><button onClick={()=>setM(!m)} className="lg:hidden" aria-label={m?"Close navigation menu":"Open navigation menu"} aria-expanded={m}><Menu/></button><Link href="/account" onClick={closeMenu} aria-label="Account"><User/></Link><button onClick={()=>setOpen(true)} className="relative" aria-label="Cart"><ShoppingBag/><span className="absolute -right-2 -top-2 rounded-full bg-pink-600 px-1.5 text-xs">{count}</span></button></div></header>}
